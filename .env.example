# Production: URL of the deployed Cloudflare Worker (see worker/README.md)
# Example: https://api.asynckeel.com  or  https://asynckeel-waitlist-worker.<subdomain>.workers.dev
PUBLIC_WAITLIST_API_URL=

# Local dev only: persist waitlist signups to a JSON file instead of in-memory store
# Example: .data/waitlist.json
WAITLIST_STORAGE_FILE=
