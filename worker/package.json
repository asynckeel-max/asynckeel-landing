{
  "name": "asynckeel-waitlist-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create asynckeel_waitlist",
    "db:migrate": "wrangler d1 execute asynckeel_waitlist --file ./schema.sql",
    "db:migrate:remote": "wrangler d1 execute asynckeel_waitlist --remote --file ./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.0"
  }
}
