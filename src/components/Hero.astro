<section class="mx-auto w-full max-w-6xl px-4 pb-16 pt-14 sm:px-6 lg:px-8 lg:pb-24 lg:pt-20">
  <div class="max-w-3xl">
    <p class="inline-flex items-center rounded-full border border-green-500/40 bg-green-500/10 px-4 py-1 text-sm text-green-300">
      🚀 Now in Public Beta
    </p>
    <h1 class="mt-6 text-4xl font-bold leading-tight tracking-tight text-white sm:text-5xl lg:text-6xl">
      Ship your backend
      <br class="hidden sm:block" />
      in hours, not weeks.
    </h1>
    <p class="mt-6 text-base leading-7 text-zinc-300 sm:text-lg">
      AsyncKeel is a production-ready FastAPI boilerplate for indie developers.
      Auth, database, rate limiting, API versioning — all wired up.
      You just build your product.
    </p>
    <div class="mt-8 flex flex-col gap-4 sm:flex-row">
      <a
        href="#pricing"
        class="inline-flex items-center justify-center rounded-lg bg-gradient-to-r from-green-500 to-emerald-400 px-6 py-3 font-semibold text-black transition hover:brightness-110"
      >Start for Free</a>
      <a
        href="https://github.com/asynckeel-max/asynckeel-core"
        class="inline-flex items-center justify-center rounded-lg border border-white/15 px-6 py-3 font-semibold text-white transition hover:border-white/35"
      >View on GitHub →</a>
    </div>
  </div>

  <div class="mt-12 max-w-3xl rounded-xl border border-white/10 bg-zinc-950 p-5 shadow-2xl shadow-black/40">
    <div class="mb-4 flex items-center gap-2">
      <span class="h-2.5 w-2.5 rounded-full bg-red-400"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-yellow-400"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-green-400"></span>
      <span class="ml-2 text-xs text-zinc-500">terminal</span>
    </div>
    <pre class="overflow-x-auto text-sm leading-7 text-zinc-200"><code>pip install asynckeel
asynckeel init my-project
cd my-project && uvicorn main:app --reload
# ✓ Auth endpoints ready
# ✓ Database connected
# ✓ API v1 live at localhost:8000</code></pre>
  </div>
</section>
